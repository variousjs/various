import React from 'react'
import { createComponent } from '@variousjs/various'

const V = createComponent({ name: 'vue-toggle', type: 'vue3' })

export default () => (
  <div>N</div>
)

export const VueC = () => <V />
